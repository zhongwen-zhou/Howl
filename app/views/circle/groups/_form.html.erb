<%= simple_form_for [:circle, @group], :validate => true do |f| %>
  <%= f.input :name, label: "圈子名", placeholder: "来一个响亮的口号吧！" %>
  <%= f.input :description, :as => :text, label: "圈子介绍", :input_html => {:rows => 5, :cols =>25} %>
  <%= f.button :submit, :value => "新建" %>
<% end %>