<div id=activity_detail_div><%= active.subject %> / <%= link_to active.user.nick_name, user_path(active.user) %> <br /> <%= active.start_date.strftime("%Y-%m-%d") %> -- <%= active.end_date.strftime("%Y-%m-%d") %>  <br />  活动状态:<%= active.running_state_name %>  <br/>  <% if @current_user.joind_activity?(active) %>  总预算：<%= active.total_budgets_sum %> / 目前支出：<%= active.total_accounts_sum %> / 当前余额：<%= active.balance_sum %>   <br />  我应该支出：<%= active.personal_outcome_account(@current_user) %>  <br />  我已经支出：<%= active.personal_outcome_total_account(@current_user) %>   <br />  我还应该支付：<%= active.personal_outcome_should_account(@current_user)%>  <br />  <% end %>  <%= link_to( "创建预算", new_circle_group_activity_budget_path(@group, active)) if @current_user.is_admin_group?(@group) %>  <%= link_to(" / 添加帐务",new_circle_group_activity_account_path(@group, active)) if @current_user.is_admin_group?(@group) %>  <%= link_to("查看帐务", circle_group_activity_accounts_path(@group, active), :remote => true) if false %>  <%= link_to(" / 参见活动",circle_group_activity_activity_users_path(@group,active), :method => :post) unless @current_user.joind_activity?(active) %>  <%= link_to(" / 退出活动",circle_group_activity_activity_users_path(@group,active), :method => :delete, data: { confirm: '你确定退出该活动么？' }) if (@current_user.joind_activity?(active) && !@current_user.is_create_group?(@group)) %></div>